import React from "react";

const Footer: React.FC = () => {
	return (
		<footer className="px-8 py-2 fixed bottom-2 left-0 right-0 flex flex-row items-center justify-between w-full"></footer>
	);
};

export default Footer;
