import * as React from "react";

import { Metadata, NextPage } from "next";

export const metadata: Metadata = {
	title: "Getting ready.",
};

const Home: NextPage = () => {
	return <></>;
};

export default Home;
